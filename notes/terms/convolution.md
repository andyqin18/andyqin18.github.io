# Convolution

A sliding-window operation where a small learnable kernel is applied across an
image to detect local patterns (edges, textures). Stacking them builds up from
low-level to high-level features.

*Used by:* [ResNet](#resnet), [Vision Transformer](#vit)
